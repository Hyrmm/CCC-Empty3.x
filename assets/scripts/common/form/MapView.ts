import { _decorator } from 'cc';
import FormView from 'db://assets/frameWork/ui/form/FormView';
const { ccclass, property } = _decorator;

@ccclass('MapView')
export class MapView extends FormView {
    protected start() {

    }

    protected update(deltaTime: number) {

    }
}